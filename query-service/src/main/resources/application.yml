spring:
  kafka:
    bootstrap-servers: kafka:9092
    streams:
      application-id: ecommerce-analytics
      properties:
        processing.guarantee: exactly_once_v2
        commit.interval.ms: 1000